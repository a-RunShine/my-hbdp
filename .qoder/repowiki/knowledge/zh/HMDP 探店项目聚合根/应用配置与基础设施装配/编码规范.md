- 配置类统一放在 `com.hmdp.config` 包下并使用 `@Configuration` + `@Bean` 方法暴露组件，而非 XML 或自动装配。
- 外部中间件（Redisson、StringRedisTemplate）通过构造参数或 `@Resource` 注入，避免在配置类内自行创建实例。
- 跨领域横切逻辑（拦截器、异常处理）集中在 config 包，通过 `addInterceptors` / `@RestControllerAdvice` 声明式注册，不对业务代码侵入。