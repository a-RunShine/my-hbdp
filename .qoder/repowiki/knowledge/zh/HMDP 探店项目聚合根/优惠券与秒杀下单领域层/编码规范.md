- Service 实现统一继承 `ServiceImpl<M, T>` 并实现对应 `I*Service` 接口，由 Spring `@Service` 注册。
- 跨表写操作（如新增秒杀券、创建订单）使用 `@Transactional` 包裹，保证多表写入的原子性。
- 对外返回统一封装 `Result.ok(...)` / `Result.fail(...)`，Controller 不直接抛异常。
- 需要事务生效的方法通过 `AopContext.currentProxy()` 获取代理后内部调用，避免同类自调绕过 AOP。