模块遵循标准分层：`entity/Follow.java` 通过 `@TableName("tb_follow")` 映射到数据库表，字段包含 `userId`（关注者）、`followUserId`（被关注者）和 `createTime`；`mapper/FollowMapper.java` 继承 `BaseMapper<Follow>` 仅声明类型，不定义自定义 SQL；`service/IFollowService` 与 `service/impl/FollowServiceImpl` 分别继承 `IService<Follow>` 与 `ServiceImpl<FollowMapper, Follow>`，复用 MyBatis-Plus 的通用 CRUD；`controller/FollowController` 目前为空骨架，仅标注 `@RestController` 与 `@RequestMapping("/follow")`，业务接口尚未实现。依赖方向单向向下：Controller → Service → Mapper → Entity，未引入缓存或外部组件。