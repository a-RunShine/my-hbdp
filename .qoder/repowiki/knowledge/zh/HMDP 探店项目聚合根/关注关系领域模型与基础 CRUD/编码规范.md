- Entity 统一使用 Lombok 注解组合 `@Data` + `@EqualsAndHashCode(callSuper = false)` + `@Accessors(chain = true)`，并通过 `@TableName` 显式指定表名。
- Service 层采用接口 + 实现分离模式，接口继承 `IService<T>`，实现类继承 `ServiceImpl<M, T>` 并标注 `@Service`，不自行重写方法。
- Mapper 接口仅继承 `BaseMapper<T>` 而不声明任何方法，完全依赖 MyBatis-Plus 提供的通用 CRUD。